import React from 'react';
import { View, Text } from 'react-native';

interface Props {}

export const Profile: React.FC<Props> = ({}) => {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
};
