/*
This should be always implement in all StyleSheets Interfaces at zIndex
*/

export enum StyleElevationIndex {
  modal = 9000,
  overlay = 8000,
  dropdown = 7000,
  header = 6000,
  footer = 5000,
}
